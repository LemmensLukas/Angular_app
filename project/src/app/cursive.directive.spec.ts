import { CursiveDirective } from './cursive.directive';

/*describe('CursiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CursiveDirective();
    expect(directive).toBeTruthy();
  });
});*/
